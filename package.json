{
  "name": "elephant-recruitment",
  "version": "1.0.0",
  "description": "Full-stack recruitment automation system",
  "scripts": {
    "dev": "concurrently -k -n backend,frontend \"cd backend-api && npm start\" \"cd frontend && npm start\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "firecrawl-mcp": "^3.5.2"
  }
}
